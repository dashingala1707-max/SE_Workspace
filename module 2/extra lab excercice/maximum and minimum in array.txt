#include <stdio.h>

int main() {
    int arr[10], i, j, temp, max, min;

    // 1. Accept 10 integers from the user
    printf("Enter 10 integers:\n");
    for(i = 0; i < 10; i++) {
        printf("Element %d: ", i + 1);
        scanf("%d", &arr[i]);
    }

    // 2. Find Maximum and Minimum
    max = arr[0];
    min = arr[0];
    for(i = 1; i < 10; i++) {
        if(arr[i] > max) max = arr[i];
        if(arr[i] < min) min = arr[i];
    }
    printf("\nMaximum value: %d", max);
    printf("\nMinimum value: %d\n", min);

    // 3. Challenge: Sort the array in ascending order (Bubble Sort)
    for(i = 0; i < 10 - 1; i++) {
        for(j = 0; j < 10 - i - 1; j++) {
            if(arr[j] > arr[j + 1]) {
                // Swap elements
                temp = arr[j];
                arr[j] = arr[j + 1];
                arr[j + 1] = temp;
            }
        }
    }

    // Print sorted array
    printf("\nSorted array in ascending order:\n");
    for(i = 0; i < 10; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");

    return 0;
}
